# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key

# multi-agent-collab runtime caches
.collab/.update-cache

# Claude Code local permission settings (per-developer; should not be shared)
.claude/settings.local.json
